{
  "compilerOptions": {
        // 设置项目的基本路径，用于解析非相对模块名称
    "baseUrl": "./",
        
    "paths": {
      // 配置别名路径可以在写代码时联想提示路径
      "@/*": ["./src/*"]
    }
  },
  "exclude": ["node_modules", "dist"]
}